# Video Orchestrator - Environment Configuration
# Copy this file to .env and fill in your actual values

# ==========================================
# AI Services API Keys
# ==========================================

# OpenAI API Key (for GPT models)
# Get your key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# Google Gemini API Key (for Gemini models)
# Get your key from: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your-gemini-api-key-here

# AI Provider (set to 'mock' for testing without real API keys)
AI_PROVIDER=mock

# ==========================================
# Stock Media API Keys (Optional)
# ==========================================

# Pexels API Key (for stock videos/images)
# Get your key from: https://www.pexels.com/api/
PEXELS_API_KEY=your-pexels-api-key-here

# Pixabay API Key (for stock videos/images)
# Get your key from: https://pixabay.com/api/docs/
PIXABAY_API_KEY=your-pixabay-api-key-here

# ==========================================
# External Video Processing API Keys (Optional)
# ==========================================

# Pictory API Key (for AI-powered script-to-video generation)
# Get your key from: https://www.pictory.ai/
# Supports: Script-to-video, voice-over, stock footage search
# Free tier available with limited monthly quota
PICTORY_API_KEY=your-pictory-api-key-here

# Kapwing API Key (for advanced video editing and composition)
# Get your key from: https://www.kapwing.com/api
# Features: Smart resize, subtitle styling, batch processing, multi-layer composition
# Free tier available with limited monthly quota
KAPWING_API_KEY=your-kapwing-api-key-here

# ==========================================
# Server Configuration
# ==========================================

# Backend server port (default: 4545)
PORT=4545

# Node environment (development, production, test)
NODE_ENV=development

# Memory Management
MEMORY_THRESHOLD=95
MEMORY_CHECK_INTERVAL=60000

# ==========================================
# Tool Paths (Optional - uses defaults if not set)
# ==========================================

# FFmpeg binary path (relative to project root)
FFMPEG_PATH=../../tools/ffmpeg/bin/ffmpeg.exe

# Piper TTS binary path (relative to project root)
PIPER_PATH=../../tools/piper/bin/piper.exe

# Whisper.cpp binary path (relative to project root)
WHISPER_PATH=../../tools/whisper/bin/main.exe

# ==========================================
# Processing Settings
# ==========================================

# Maximum video duration in seconds (default: 60)
MAX_VIDEO_DURATION=60

# Default video FPS (default: 30)
DEFAULT_VIDEO_FPS=30

# Default video resolution (default: 1080x1920 for 9:16 aspect ratio)
DEFAULT_VIDEO_RESOLUTION=1080x1920

# Audio loudness target in LUFS (default: -16)
AUDIO_LOUDNESS_TARGET=-16

# ==========================================
# Data Directories (Optional - uses defaults)
# ==========================================

# Root data directory (default: ../../data)
# DATA_DIR=../../data

# Cache directory (default: ../../data/cache)
# CACHE_DIR=../../data/cache

# Export directory (default: ../../data/exports)
# EXPORT_DIR=../../data/exports

# ==========================================
# Logging Configuration
# ==========================================

# Log level (error, warn, info, debug)
LOG_LEVEL=info

# ==========================================
# Processing Limits
# ==========================================

# Maximum file upload size in MB (default: 500MB)
MAX_UPLOAD_SIZE=500

# Maximum video duration in seconds (default: 180)
MAX_VIDEO_DURATION=180

# ==========================================
# Notes
# ==========================================
# 
# 1. NEVER commit the actual .env file to version control
# 2. Keep API keys secret and secure
# 3. Restart the backend server after changing .env values
# 4. AI services will use mock responses if API keys are not set
# 5. Tool paths are relative to the project root directory
# 6. All directories will be created automatically if they don't exist