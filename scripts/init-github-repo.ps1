# Navigate to the project directory
cd C:\path\to\your\project

# Initialize a Git repository
git init

# Configure Git user name and email
git config user.name "Your Name"
git config user.email "your.email@example.com"

# NOTE: Replace the path, name and email above with your actual values before running this script.
# You can run this script in PowerShell:
#   powershell -ExecutionPolicy Bypass -File scripts\init-github-repo.ps1
