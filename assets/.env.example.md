În regulă, Gabor — îți refac **fișierul `.env.example`** astfel încât să includă **toate variabilele de mediu** necesare pentru:

- rularea aplicației Video Orchestrator în mediu local  
- execuția suitei de teste (unit, integration, E2E, media validation)  
- integrarea în pipeline CI/CD  

Acest fișier poate fi pus direct în repo ca referință pentru configurarea `.env` real.

---

## 📄 **`.env.example`**

```env
##############################################
# 🔐 API KEYS – Servicii externe AI
##############################################

# Cheie API OpenAI (pentru generarea scripturilor)
OPENAI_API_KEY=your-openai-key-here

# Cheie API Google Gemini (alternativă AI)
GEMINI_API_KEY=your-gemini-key-here

##############################################
# 🎙️ CONFIGURARE TTS LOCAL (Piper / Coqui)
##############################################

# Calea către modelul Piper (.onnx)
PIPER_MODEL_PATH=tools/piper/models/en-us.onnx

# Vocea implicită pentru TTS
PIPER_VOICE=en_us-ryan-medium

##############################################
# 📝 CONFIGURARE WHISPER (Speech-to-Text)
##############################################

# Calea către modelul Whisper (.bin)
WHISPER_MODEL_PATH=tools/whisper/models/ggml-base.en.bin

##############################################
# 🎥 CONFIGURARE FFMPEG
##############################################

# Calea către executabilul FFmpeg (dacă nu e în PATH)
FFMPEG_PATH=ffmpeg

##############################################
# 📦 DIRECTOARE DE LUCRU
##############################################

# Director export fișiere finale
EXPORT_DIR=data/exports

# Director cache temporar
CACHE_DIR=data/cache

##############################################
# 🧪 CONFIGURARE TESTARE
##############################################

# Director fixture-uri media pentru teste
TEST_FIXTURE_DIR=tests/media-fixtures

# Director export teste
TEST_EXPORT_DIR=tests/test-exports

# Timeout global teste (ms)
TEST_TIMEOUT_MS=30000

# Activează mock AI în teste (true/false)
ENABLE_MOCK_AI=false

##############################################
# 🌐 CONFIGURARE SERVER BACKEND
##############################################

# Port backend Node.js
PORT=3000

# Host backend
HOST=localhost

##############################################
# 🖥️ CONFIGURARE UI TAURI
##############################################

# Port dev UI Tauri
TAURI_DEV_PORT=1420

##############################################
# 🛠️ LOGGING & DEBUG
##############################################

# Nivel logare (error, warn, info, debug)
LOG_LEVEL=info
```

---

## 🔍 **Cum se folosește**
1. Copiază acest fișier în root-ul proiectului și redenumește-l `.env`.
2. Completează valorile reale (chei API, căi absolute dacă e nevoie).
3. În CI/CD, setează aceste variabile în interfața platformei (GitHub Actions, GitLab CI etc.).
4. Pentru testare locală, asigură-te că toate căile către modele și tool-uri sunt valide.

---

💡 Dacă vrei, îți pot face și **un script `env-check.js`** care să verifice automat înainte de build/test dacă toate variabilele din `.env` sunt setate corect și să afișeze ce lipsește.  
Vrei să îți pregătesc și acel script de validare?